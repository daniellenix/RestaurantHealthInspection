<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.sfu.prjCalcium.pr1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.InspectionActivity"
            android:label="Inspection Information">
        </activity>
        <activity
            android:name=".UI.RestaurantDetailActivity"
            android:label="Restaurant Information"/>
        <activity
            android:name=".UI.RestaurantListActivity"
            android:label="List of Restaurants">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>